"""Utilities: camera poses, history pruning, video I/O, trajectory authoring.

Submodules are imported explicitly (e.g. `from gim.utils import camera`) so
that light-weight tools do not pull in torch / scipy.
"""
